# API Configuration for Development
# This ensures Next.js SSR fetches go directly to Express (port 3001), not back to Next.js (port 5000)

# Express API URL for server-side requests
EXPRESS_URL=http://127.0.0.1:3001

# Public site URL (for SEO, OG tags, etc.)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Note: NEXT_PUBLIC_EXPRESS_URL is not needed
# Client-side uses relative URLs (/api/*) via Next.js rewrites
